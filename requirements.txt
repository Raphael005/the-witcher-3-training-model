# Pinned runtime dependencies. For development install via `pip install -e .[dev]`.
torch>=2.3
transformers>=4.44
trl>=0.11
datasets>=2.20
accelerate>=0.33
bitsandbytes>=0.43; platform_system != "Darwin"
pyyaml>=6.0
